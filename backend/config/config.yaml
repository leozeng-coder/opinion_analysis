server:
  port: "8080"
  mode: "debug"   # debug | release

database:
  dsn: "root:123456@tcp(127.0.0.1:3306)/opinion_analysis?charset=utf8mb4&parseTime=True&loc=Local"
  maxOpenConn: 100
  maxIdleConn: 10

redis:
  addr: "127.0.0.1:6379"
  password: ""
  db: 0

jwt:
  secret: "opinion-analysis-secret-key-change-in-prod"
  expireHour: 24

log:
  level: "info"
  filename: "logs/app.log"

# 前端「立即爬取」由后端在本机拉起 MindSpider/run_once.py；Docker 纯后端镜像无 Python 时请改为 enabled: false
crawler:
  enabled: true
  root: "../MindSpider-main"
  python: "C:\\Users\\leozeng\\AppData\\Local\\Programs\\Python\\Python313\\python.exe"
